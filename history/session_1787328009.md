## Session Summary

- Replaced Bits UI Select on the AI Assistant creation modal with a custom Svelte dropdown to ensure viewroom checkboxes render correctly and can be selected.
- Updated `/api/ai/chat/+server.ts` so the `roomKey` matches against `roomId` OR `roomName` allowing AI bots to be active and function perfectly in connected Viewrooms.
